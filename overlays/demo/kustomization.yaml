apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: demo-rdepot

resources:
  - ../../base/app-stateful
  - ../../base/repo
  - ../../base/client-frontend
  - resources/rdepot-db.service.yaml
  - resources/rdepot-db.statefulset.yaml

patches:
  - path: patches/rdepot-app.statefulset.yaml
  - path: patches/rdepot-repo.statefulset.yaml
  - path: patches/rdepot-db-init.job.yaml

configMapGenerator:
  - name: rdepot-app
    files:
      - config/application.yml
      - config/authentication.yml
    behavior: replace

secretGenerator:
  - literals:
      - username=rdepot
      - password=rdepot123
    name: rdepot-db-credentials
